{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/gulfofmaine/odp-releaser/main/schemas/image_manifest.schema.json",
  "title": "odp-releaser image manifest",
  "description": "Configuration read by `odp-releaser bump-images`, mapping image names to the manifests each should update on release.",
  "$defs": {
    "AllowedActors": {
      "description": "Actors allowed to trigger a config's bumps. Both lists empty denies everyone.",
      "properties": {
        "users": {
          "description": "GitHub usernames, compared case-insensitively against the payload's source actor",
          "items": {
            "type": "string"
          },
          "title": "Users",
          "type": "array"
        },
        "teams": {
          "description": "GitHub teams as org/team-slug entries. Membership is checked with the source org's reporter app credentials (REPORTER_APPS / REPORTER_APP_ID / REPORTER_APP_PRIVATE_KEY), so that app must also be granted the organization Members: read permission",
          "items": {
            "type": "string"
          },
          "title": "Teams",
          "type": "array"
        }
      },
      "title": "AllowedActors",
      "type": "object",
      "additionalProperties": false
    },
    "CommentConfig": {
      "description": "Comment posted back on the source pull request when this image is bumped.\n\nEvery field is optional and inherited independently: an unset field takes\nthe ``defaults``-level value, then the built-in template. See\n:func:`resolve_comment_config`.",
      "properties": {
        "enabled": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Whether to comment on the source pull request for this config's bumps. Unset inherits the defaults-level value, which itself defaults to true",
          "title": "Enabled"
        },
        "staged": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Comment body posted while a pull_request-mode bump is open and awaiting review. May reference the comment placeholders (see the docs); escape literal braces as `{{`/`}}`. Unset inherits the defaults-level value, then the built-in template",
          "title": "Staged"
        },
        "deployed": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Comment body posted once the bump has landed -- immediately for a commit-mode bump, or when the bump pull request merges. Unset inherits the defaults-level value, then the built-in template",
          "title": "Deployed"
        }
      },
      "title": "CommentConfig",
      "type": "object",
      "additionalProperties": false
    },
    "ConfigDefaults": {
      "description": "Default settings for every image config; a config's own value replaces the default.",
      "properties": {
        "allowed_source_repos": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Full repo names (owner/name) allowed to trigger bumps; None disables the check",
          "title": "Allowed Source Repos"
        },
        "allowed_actors": {
          "anyOf": [
            {
              "$ref": "#/$defs/AllowedActors"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Users and teams allowed to trigger bumps; None disables the check"
        },
        "environment": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Default GitHub environment name used when reporting deployments back to source repos (`report-deployment`). Overridable per image config; unset falls back to this deploy repo's owner/name slug",
          "title": "Environment"
        },
        "environment_url": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Default URL reported as the deployment's 'View deployment' link. May reference `{new_tag}`, `{git_sha}`, and `{digest}`. Overridable per image config; unset falls back to the bump commit or pull request URL",
          "title": "Environment Url"
        },
        "comment": {
          "anyOf": [
            {
              "$ref": "#/$defs/CommentConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Default comment posted back on source pull requests when images are bumped. Overridable per image config, field by field; unset falls back to the built-in templates"
        },
        "reviewers": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "GitHub usernames requested as reviewers on bump pull requests",
          "title": "Reviewers"
        },
        "team_reviewers": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "GitHub team slugs (no org prefix) requested as reviewers on bump pull requests. The bump-images workflow detects this key and mints its app token with organization Members: read (the ci app must be granted the permission)",
          "title": "Team Reviewers"
        },
        "sync": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Default for whether odp-releaser copies the payload's image to a config's deployed_as before the bump lands. Overridable per image config; unset falls back to false.",
          "title": "Sync"
        }
      },
      "title": "ConfigDefaults",
      "type": "object",
      "additionalProperties": false
    },
    "FileManifest": {
      "description": "A generic YAML or JSON manifest updated purely via ``set`` paths.\n\nUnlike the kustomize and helm manifests there is no implicit image field\nto update, so a bare-string form carries no useful information; the mapping\nform with an explicit ``set`` is required.",
      "properties": {
        "path": {
          "description": "Relative path to the file manifest",
          "format": "path",
          "title": "Path",
          "type": "string"
        },
        "set": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Mapping of yamlpath expressions to templated values. Values may reference `{new_tag}`, `{git_sha}`, `{digest}`, and `{payload}`",
          "title": "Set",
          "type": "object"
        }
      },
      "required": [
        "path",
        "set"
      ],
      "title": "FileManifest",
      "type": "object",
      "additionalProperties": false
    },
    "HelmManifest": {
      "description": "Helm manifest configuration with Dagster user deployments chart layout shorthand.",
      "properties": {
        "path": {
          "description": "Relative path to the Helm values file",
          "format": "path",
          "title": "Path",
          "type": "string"
        },
        "dagster_user_code": {
          "default": false,
          "description": "When true, update the image.tag of every entry in the top-level 'deployments' list whose image.repository matches the released image (Dagster user-deployments chart layout)",
          "title": "Dagster User Code",
          "type": "boolean"
        },
        "set": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Mapping of yamlpath expressions to templated values. Values may reference `{new_tag}`, `{git_sha}`, `{digest}`, and `{payload}`",
          "title": "Set",
          "type": "object"
        }
      },
      "required": [
        "path"
      ],
      "title": "HelmManifest",
      "type": "object",
      "additionalProperties": false
    },
    "ImageConfig": {
      "description": "Configuration for an image, specifying which manifests to update and how.",
      "properties": {
        "events": {
          "anyOf": [
            {
              "items": {
                "enum": [
                  "push",
                  "publish",
                  "workflow_dispatch",
                  "release"
                ],
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "List of GitHub events for these manifests. Only these events will trigger updates. If `None`, all events trigger updates.",
          "title": "Events"
        },
        "allowed_source_repos": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Full repo names (owner/name) allowed to trigger this config. Replaces the defaults-level list; unset inherits it. A config whose resolved list rejects the payload's repo is skipped",
          "title": "Allowed Source Repos"
        },
        "allowed_actors": {
          "anyOf": [
            {
              "$ref": "#/$defs/AllowedActors"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Users and teams allowed to trigger this config. Replaces the defaults-level setting; unset inherits it. A config whose resolved actors reject the payload's actor is skipped. Use YAML merge keys (<<: *anchor) to share allowlists between configs"
        },
        "environment": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "GitHub environment name reported back to the source repo for this config's bumps (`report-deployment`). Overrides the defaults-level environment; unset falls back to the deploy repo's owner/name slug",
          "title": "Environment"
        },
        "environment_url": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "URL reported as the deployment's 'View deployment' link, e.g. where this config's app runs. May reference `{new_tag}`, `{git_sha}`, and `{digest}`. Overrides the defaults-level environment_url; unset falls back to the bump commit or pull request URL",
          "title": "Environment Url"
        },
        "comment": {
          "anyOf": [
            {
              "$ref": "#/$defs/CommentConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Comment posted back on the source pull request for this config's bumps. Each field is inherited from the defaults-level setting independently, so overriding one template keeps the other"
        },
        "update_mode": {
          "default": "commit",
          "description": "Whether to commit the change directly or open a pull request for review",
          "enum": [
            "commit",
            "pull_request"
          ],
          "title": "Update Mode",
          "type": "string"
        },
        "reviewers": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "GitHub usernames requested as reviewers when this config's bump opens a pull request. Replaces the defaults-level list; unset inherits it, [] requests none",
          "title": "Reviewers"
        },
        "team_reviewers": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "GitHub team slugs (no org prefix) requested as reviewers on the bump pull request. Replaces the defaults-level list. The bump-images workflow detects this key and mints its app token with organization Members: read",
          "title": "Team Reviewers"
        },
        "deployed_as": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The image name the manifests under this config actually deploy from, when it differs from the payload's image_name -- e.g. an ECR pull-through cache path such as '705162855742.dkr.ecr.us-east-1.amazonaws.com/docker-hub/gmri/sea-eagle-brown-3crs' mirroring upstream 'gmri/sea-eagle-brown-3crs'. Used as the Helm dagster shorthand's image.repository selector and in `set` templating via `{deployed_image}`; kustomize's own `newName` already carries the mirror, so this does not change which `images:` entry kustomize matches. Per-config only",
          "title": "Deployed As"
        },
        "sync": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Whether odp-releaser copies the payload's image to deployed_as before the bump lands. Unset or false means declare-only: the correct setting for ECR pull-through and any other registry-native replication, where there is nothing to copy because the cache populates itself on the next pull. Overrides the defaults-level value; unset inherits it, then falls back to false",
          "title": "Sync"
        },
        "kustomize_manifests": {
          "description": "List of Kustomize manifests to set for the image",
          "items": {
            "$ref": "#/$defs/KustomizeManifest"
          },
          "title": "Kustomize Manifests",
          "type": "array"
        },
        "helm_charts": {
          "description": "List of Helm values files to update for the image",
          "items": {
            "$ref": "#/$defs/HelmManifest"
          },
          "title": "Helm Charts",
          "type": "array"
        },
        "file_manifests": {
          "description": "List of generic YAML or JSON manifests updated via set paths",
          "items": {
            "$ref": "#/$defs/FileManifest"
          },
          "title": "File Manifests",
          "type": "array"
        }
      },
      "title": "ImageConfig",
      "type": "object",
      "additionalProperties": false
    },
    "KustomizeManifest": {
      "anyOf": [
        {
          "description": "Kustomize manifest configuration. Updates the image overrides and set fields.",
          "properties": {
            "path": {
              "description": "Relative path to the Kustomize manifest",
              "format": "path",
              "title": "Path",
              "type": "string"
            },
            "set": {
              "additionalProperties": {
                "type": "string"
              },
              "description": "Mapping of yamlpath expressions to templated values. Values may reference `{new_tag}`, `{git_sha}`, `{digest}`, and `{payload}`",
              "title": "Set",
              "type": "object"
            },
            "pin": {
              "default": "tag",
              "description": "Whether the kustomize images entry pins the tag (newTag) or the immutable digest (digest)",
              "enum": [
                "tag",
                "digest"
              ],
              "title": "Pin",
              "type": "string"
            }
          },
          "required": [
            "path"
          ],
          "title": "KustomizeManifest",
          "type": "object",
          "additionalProperties": false
        },
        {
          "description": "Relative path to the Kustomize manifest, shorthand for a mapping with only `path` set",
          "type": "string"
        }
      ]
    }
  },
  "properties": {
    "defaults": {
      "$ref": "#/$defs/ConfigDefaults",
      "description": "Default settings applied to every image config; a config's own value replaces the default"
    },
    "images": {
      "additionalProperties": {
        "items": {
          "$ref": "#/$defs/ImageConfig"
        },
        "type": "array"
      },
      "description": "Mapping of image names to their configurations",
      "title": "Images",
      "type": "object"
    }
  },
  "required": [
    "images"
  ],
  "type": "object",
  "additionalProperties": false,
  "patternProperties": {
    "^x-": true
  }
}
