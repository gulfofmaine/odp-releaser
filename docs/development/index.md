---
icon: lucide/hammer
---

# Development

How to work on `odp-releaser` itself — set up an environment, run the checks,
and build the docs. If you're setting up a repo that *uses* odp-releaser, you
want [Getting started](../getting-started.md) instead.

--8<-- ".github/CONTRIBUTING.md:contributing"

## Exercising a config

Both config files can be dry-run against canned payloads without touching
GitHub — see [Testing](testing.md), which also covers how this repo's own CI
exercises both reusable workflows end-to-end on every pull request.
